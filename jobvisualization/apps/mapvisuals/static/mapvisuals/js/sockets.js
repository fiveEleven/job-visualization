// var socket = io.connect()

// (Action) Emit to server event name and object.
// socket.emit('event_name', {key: 'value'}) 

// (Listener) On 'event_name'
// socket.on('event_name', function(users){
	// Code...
// })